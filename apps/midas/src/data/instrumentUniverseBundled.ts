// This file is intentionally versioned as the single repo slot for the official
// Instrument Universe V1 backup. Keep it null until an approved JSON source is
// validated through `npm -w apps/midas run instrument-universe:update-bundled -- <path>`.
export const BUNDLED_INSTRUMENT_UNIVERSE_FILE_NAME = 'instrument-universe.bundled.json';
export const BUNDLED_INSTRUMENT_UNIVERSE_SAVED_AT: string | null = null;
export const BUNDLED_INSTRUMENT_UNIVERSE_RAW: string | null = null;
