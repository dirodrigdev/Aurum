// Change only when M8's functional calculation or metric semantics change.
// It deliberately does not track builds, commits, or deployment timestamps.
export const M8_ENGINE_REVISION = 'm8-2026-07-risk-quality-v1' as const;
