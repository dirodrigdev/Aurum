# PATCH PACK L – Trips cards legibility (Finalizado)
- Fix: prevent CSS filter from affecting text by moving grayscale/brightness filter to background layer only.
- File modified: src/pages/Trips.tsx
Apply: overwrite the file in your repo, commit, push.
